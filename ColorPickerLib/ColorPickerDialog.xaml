<Window
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  x:Class="Microsoft.Samples.CustomControls.ColorPickerDialog"
  xmlns:customControls="clr-namespace:Microsoft.Samples.CustomControls"
  Title="Select a Color"
  Width="600"
    WindowStartupLocation="CenterOwner"
  Height="420" ResizeMode="NoResize" WindowStyle="None">
    <Border BorderThickness="2" BorderBrush="DarkGray">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom"
      Orientation="Horizontal"
      HorizontalAlignment="Right" 
      Margin="10">
                <Button Name="OKButton" Padding="5" Margin="5,0,5,0"
        Click="okButtonClicked"
        IsEnabled="False">OK</Button>
                <Button Click="cancelButtonClicked" Margin="5,0,5,0" Padding="5">Cancel</Button>
            </StackPanel>

            <customControls:ColorPicker 
      x:Name="cPicker"
       SelectedColorChanged="onSelectedColorChanged" 
       Margin="10,10,10,0"/>
        </DockPanel>
    </Border>

</Window>
