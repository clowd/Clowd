[package]
name = "clowd_shell_ext"
version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib"]

# The implementation is entirely #[cfg(windows)]; on other targets the crate
# builds as an empty library so workspace-wide builds (macOS CI) stay green.
[target.'cfg(windows)'.dependencies]
windows = { workspace = true, features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_Com",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_UI_Shell",
    "Win32_UI_WindowsAndMessaging",
] }
# direct dependency because code generated by #[implement] references ::windows_core paths
windows-core = "0.62"
