<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Common.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="DefaultTexBox" TargetType="{x:Type TextBox}">
        <Setter Property="FontSize" Value="{StaticResource ControlsFontSize}" />
        <Setter Property="FontWeight" Value="{StaticResource ControlsFontWeight}" />
        <Setter Property="FontFamily" Value="{StaticResource ControlsFontFamily}" />
        <Setter Property="Padding" Value="{StaticResource ControlsPadding}" />
        <Setter Property="Margin" Value="{StaticResource ControlsMargin}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBoxForeground}" />
        <Setter Property="Background" Value="{DynamicResource TextBoxOuterBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxOuterBorder}" />
        <Setter Property="BorderThickness" Value="{StaticResource ControlsBorderThickness}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border
                        x:Name="OuterBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource ControlsCornerRadius}"
                        SnapsToDevicePixels="True">
                        <Border
                            x:Name="InnerBorder"
                            Background="{DynamicResource TextBoxInnerBackground}"
                            BorderBrush="{DynamicResource TextBoxInnerBorder}"
                            BorderThickness="0"
                            SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource HoverTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource HoverTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource HoverTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource HoverTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource HoverTextBoxForeground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter Property="Background" Value="{DynamicResource FocusedTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource FocusedTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource FocusedTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource FocusedTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource FocusedTextBoxForeground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource DisabledTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource DisabledTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource DisabledTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource DisabledTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource DisabledTextBoxForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource DefaultTexBox}" TargetType="{x:Type TextBox}" />
    <Style
        x:Key="{x:Static ToolBar.TextBoxStyleKey}"
        BasedOn="{StaticResource DefaultTexBox}"
        TargetType="TextBox">
        <Setter Property="FontSize" Value="{StaticResource ControlsFontSizeSmall}" />
        <Setter Property="Margin" Value="0" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="FontSize" Value="{StaticResource ControlsFontSize}" />
        <Setter Property="FontWeight" Value="{StaticResource ControlsFontWeight}" />
        <Setter Property="FontFamily" Value="{StaticResource ControlsFontFamily}" />
        <Setter Property="Foreground" Value="{DynamicResource TextBoxForeground}" />
        <Setter Property="Background" Value="{DynamicResource TextBoxOuterBackground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxOuterBorder}" />
        <Setter Property="BorderThickness" Value="{StaticResource ControlsBorderThickness}" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="{StaticResource ControlsPadding}" />
        <Setter Property="Margin" Value="{StaticResource ControlsMargin}" />
        <Setter Property="AllowDrop" Value="true" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Border
                        x:Name="OuterBorder"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{StaticResource ControlsCornerRadius}">
                        <Border
                            x:Name="InnerBorder"
                            Background="{DynamicResource TextBoxInnerBackground}"
                            BorderBrush="{DynamicResource TextBoxInnerBorder}"
                            BorderThickness="0">
                            <ScrollViewer x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Background" Value="{DynamicResource HoverTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource HoverTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource HoverTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource HoverTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource HoverTextBoxForeground}" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="true">
                            <Setter Property="Background" Value="{DynamicResource FocusedTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource FocusedTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource FocusedTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource FocusedTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource FocusedTextBoxForeground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="{DynamicResource DisabledTextBoxOuterBackground}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource DisabledTextBoxOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{DynamicResource DisabledTextBoxInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{DynamicResource DisabledTextBoxInnerBorder}" />
                            <Setter Property="Foreground" Value="{DynamicResource DisabledTextBoxForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>





</ResourceDictionary>