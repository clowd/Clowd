<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Common.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!--  NORMAL  -->
    <SolidColorBrush x:Key="ScrollBarBackground" Color="Transparent" />
    <SolidColorBrush x:Key="ScrollBarHorizontalBackground" Color="Transparent" />
    <SolidColorBrush x:Key="ScrollBarBorder" Color="Transparent" />

    <!--  DISABLED  -->
    <SolidColorBrush x:Key="DisabledScrollBarBackground" Color="Transparent" />
    <SolidColorBrush x:Key="DisabledScrollBarHorizontalBackground" Color="Transparent" />
    <SolidColorBrush x:Key="DisabledScrollBarBorder" Color="Transparent" />


    <Style x:Key="ScrollBarLineButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border
                        Name="OuterBorder"
                        Margin="0"
                        Background="{StaticResource ButtonOuterBackground}"
                        BorderBrush="{StaticResource ButtonOuterBorder}"
                        BorderThickness="1">
                        <Border
                            Name="InnerBorder"
                            Margin="0"
                            Background="{StaticResource ButtonInnerBackground}"
                            BorderBrush="{StaticResource ButtonInnerBorder}">
                            <Path
                                x:Name="Arrow"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Data="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}"
                                Fill="{StaticResource ButtonForeground}" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource HoverButtonOuterBackground}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource HoverButtonOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{StaticResource HoverButtonInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource HoverButtonInnerBorder}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource HoverButtonForeground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource PressedButtonOuterBackground}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource PressedButtonOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{StaticResource PressedButtonInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource PressedButtonInnerBorder}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource PressedButtonForeground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource DisabledButtonOuterBackground}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource DisabledButtonOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{StaticResource DisabledButtonInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource DisabledButtonInnerBorder}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource DisabledButtonForeground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarPageButton" TargetType="{x:Type RepeatButton}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border Background="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Border
                        Name="OuterBorder"
                        Background="{StaticResource ButtonOuterBackground}"
                        BorderBrush="{StaticResource ButtonOuterBorder}"
                        BorderThickness="1">
                        <Border
                            Name="InnerBorder"
                            Background="{StaticResource ButtonInnerBackground}"
                            BorderBrush="{StaticResource ButtonInnerBorder}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource HoverButtonOuterBackground}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource HoverButtonOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{StaticResource HoverButtonInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource HoverButtonInnerBorder}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="OuterBorder" Property="Background" Value="{StaticResource DisabledButtonOuterBackground}" />
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="{StaticResource DisabledButtonOuterBorder}" />
                            <Setter TargetName="InnerBorder" Property="Background" Value="{StaticResource DisabledButtonInnerBackground}" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="{StaticResource DisabledButtonInnerBorder}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="VerticalScrollBar" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition MaxHeight="18" />
                <RowDefinition Height="0.00001*" />
                <RowDefinition MaxHeight="18" />
            </Grid.RowDefinitions>
            <Border
                x:Name="BorderBackground"
                Grid.RowSpan="3"
                Background="{StaticResource ScrollBarBackground}"
                BorderBrush="{StaticResource ScrollBarBorder}"
                BorderThickness="1" />
            <RepeatButton
                Grid.Row="0"
                Height="18"
                Margin="0,0,0,0"
                Command="ScrollBar.LineUpCommand"
                Content="M 0 4 L 8 4 L 4 0 Z"
                Style="{StaticResource ScrollBarLineButton}" />
            <Track
                Name="PART_Track"
                Grid.Row="1"
                IsDirectionReversed="true">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource ScrollBarPageButton}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Margin="0,1" Style="{StaticResource ScrollBarThumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource ScrollBarPageButton}" />
                </Track.IncreaseRepeatButton>
            </Track>
            <RepeatButton
                Grid.Row="3"
                Height="18"
                Command="ScrollBar.LineDownCommand"
                Content="M 0 0 L 4 4 L 8 0 Z"
                Style="{StaticResource ScrollBarLineButton}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter TargetName="BorderBackground" Property="BorderBrush" Value="{StaticResource DisabledScrollBarBorder}" />
                <Setter TargetName="BorderBackground" Property="Background" Value="{StaticResource DisabledScrollBarBackground}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="HorizontalScrollBar" TargetType="{x:Type ScrollBar}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MaxWidth="18" />
                <ColumnDefinition Width="0.00001*" />
                <ColumnDefinition MaxWidth="18" />
            </Grid.ColumnDefinitions>
            <Border
                x:Name="BorderBackground"
                Grid.ColumnSpan="3"
                Background="{StaticResource ScrollBarHorizontalBackground}"
                BorderBrush="{StaticResource ScrollBarBorder}"
                BorderThickness="1" />
            <RepeatButton
                Grid.Column="0"
                Width="18"
                Command="ScrollBar.LineLeftCommand"
                Content="M 4 0 L 4 8 L 0 4 Z"
                Style="{StaticResource ScrollBarLineButton}" />
            <Track
                Name="PART_Track"
                Grid.Column="1"
                IsDirectionReversed="False">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageLeftCommand" Style="{StaticResource ScrollBarPageButton}" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Margin="1,0" Style="{StaticResource ScrollBarThumb}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Command="ScrollBar.PageRightCommand" Style="{StaticResource ScrollBarPageButton}" />
                </Track.IncreaseRepeatButton>
            </Track>
            <RepeatButton
                Grid.Column="3"
                Width="18"
                Command="ScrollBar.LineRightCommand"
                Content="M 0 0 L 4 4 L 0 8 Z"
                Style="{StaticResource ScrollBarLineButton}" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter TargetName="BorderBackground" Property="BorderBrush" Value="{StaticResource DisabledScrollBarBorder}" />
                <Setter TargetName="BorderBackground" Property="Background" Value="{StaticResource DisabledScrollBarHorizontalBackground}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="{x:Type ScrollBar}" TargetType="{x:Type ScrollBar}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Width" Value="Auto" />
                <Setter Property="Height" Value="18" />
                <Setter Property="Template" Value="{StaticResource HorizontalScrollBar}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="18" />
                <Setter Property="Height" Value="Auto" />
                <Setter Property="Template" Value="{StaticResource VerticalScrollBar}" />
            </Trigger>
        </Style.Triggers>
    </Style>



</ResourceDictionary>